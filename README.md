# takehome-quant

## Getting Started  
I used a virtual environment, in your shell write 
```bash
python3 -m venv venv
source venv/bin/activate
pip3 install -r requirements.txt
```
The report is in the root directory, named REPORT.pdf